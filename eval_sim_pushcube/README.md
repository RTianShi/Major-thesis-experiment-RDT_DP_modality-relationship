This package is the dedicated workspace for PushCube-specific simulation
mutations.

Its layout mirrors `eval_sim` so task-specific logic can evolve independently
without copying the entire evaluation stack.
