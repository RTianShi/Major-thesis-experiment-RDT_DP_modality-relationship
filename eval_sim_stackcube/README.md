This package is the dedicated workspace for StackCube-specific simulation mutations.

Initial layout mirrors `eval_sim` so task-specific logic can move here incrementally
without copying the whole evaluation stack up front.
